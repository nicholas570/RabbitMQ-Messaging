## Run

To see RabbitMQ management interface, you can run the following Docker command:

```bash
docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:management
```

Credentials: guest / guest
